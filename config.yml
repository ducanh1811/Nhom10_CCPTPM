version: 2.1

jobs:
    build:
        docker:
            - image: circleci/node:14-browsers
        environment:
            NODE_OPTIONS: --max-old-space-size=4096
        steps:
            - checkout
            - run: npm install
            - run: npm test

workflows:
    version: 2.1
    build-and-test:
        jobs:
            - build
